#include<stdio.h>
#include<math.h>
main()
{
	int n,k,i=0,m,sum=0;
	printf("enter a number:");
	scanf("%d",&n);
	m = n;
	while(n>0)
	{
		k = n%10;
		
	}
	printf("%d \n",sum);
    if(m==sum)
    printf("it is palindrome");
    else
    printf("it is not a palindrome");

}
